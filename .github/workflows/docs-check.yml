# https://help.github.com/en/categories/automating-your-workflow-with-github-actions

name: "Check Docs"

on:
    pull_request:
    push:
        branches:
            - "[0-9]+.[0-9]+.x"

jobs:
    checkdocs:
        uses: patchlevel/.github/.github/workflows/docs-check.yml@main
